export const DEFAULT_DETECTION_RETRIEVAL_LIMIT = 20;
export const DUPLICATE_BODY_THRESHOLD = 0.5;
export const MINIMUM_OVERLAP_THRESHOLD = 0.15;
/**
 * Pre-benchmark placeholder for the minimum cosine similarity a semantic-only
 * match needs to surface as `related`. Pending the retrieval benchmark like
 * the other detection defaults.
 */
export const RELATED_SEMANTIC_SIMILARITY_CUTOFF = 0.45;
