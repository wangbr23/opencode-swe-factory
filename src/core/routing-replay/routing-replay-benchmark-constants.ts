export const ROUTING_REPLAY_BENCHMARK_SCHEMA_VERSION = 1 as const;

export const ROUTING_REPLAY_BENCHMARK_CONSTANTS = Object.freeze({
  millisecondsPerDay: 86_400_000,
  executionLatencyMs: 1_000,
});
